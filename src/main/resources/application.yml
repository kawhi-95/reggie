Reggieserver:
  port: 8080
spring:
  application:
    #应用的名称，可选
    name: reggie
#  datasource:
#    druid:
#      driver-class-name: com.mysql.cj.jdbc.Driver
#      url: jdbc:mysql://81.69.248.154:3306/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
#      username: gjk # 数据库用户名
#      password: Aa123456    # 数据库密码
  shardingsphere:
    datasource:
      names:
        mysql-master, mysql-slave
      mysql-master:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://81.69.248.154:3301/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: 123456
      mysql-slave:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://81.69.248.154:3302/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: 123456
    masterslave:
      name: masterSlave
      load-balance-algorithm-type: round_robin
      master-data-source-name: mysql-master
      slave-data-source-names: mysql-slave
    props:
      sql:
        show: true
  main:
    allow-bean-definition-overriding: true

  redis:
    host: 81.69.248.154 # Redis地址
    port: 6379        # Redis端口
    timeout: 20000    #连接超时时间
    password: Aa123456,
    database: 0
  cache:
    redis:
      time-to-live: 1800000  # 设置过期时间  ms


mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
# 外卖项目的图片存放地址
reggie:
  # 这里目前使用的是是绝对路径
  images: backend/upload/
